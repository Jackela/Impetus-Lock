# Impetus Lock Server

FastAPI backend for Impetus Lock - an un-deletable task pressure system.

## Development

```bash
# Install dependencies
poetry install

# Run server
poetry run uvicorn server.main:app --reload
```
