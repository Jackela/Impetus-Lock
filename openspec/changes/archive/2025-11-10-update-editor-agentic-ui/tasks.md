# Tasks
- [x] Update TS types (`api.generated`, `interventionClient`) to expose action/source metadata
- [x] Refactor lock infrastructure (ContentInjector, LockManager, TransactionFilter, decorations) for inline locks and range rewrites
- [x] Implement new Muse/Loki Vibe styling (CSS + icon component) and tag DOM nodes with `data-source`
- [x] Adjust EditorCore + hooks to execute actions (provoke/rewrite/delete) and drive sensory feedback
- [x] Refresh Vitest + Playwright suites (including record-demo script) to cover rewrite/delete and Vibe UI
- [x] Validate spec with `openspec validate update-editor-agentic-ui --strict`
